	<div class="form-group"> 
			 	<% if user_signed_in? %>
			    <%=  render 'components/tweet_form' %>
			    <% else %>
			    <%= link_to 'Register now and Write something Amazing!', new_user_registration_path, class: 'badge badge-success' %>
			  <% end %>
  </div>