 <%= form_with(model: new_tweet, local: true) do |form| %>
  <% if new_tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(new_tweet.errors.count, "error") %> prohibited this new_tweet from being saved:</h2>

      <ul>
      <% new_tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <span>Add Image</span>
    <%= form.file_field :image, multipart: true, class: 'btn btn-success'   %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :new_tweet_content, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
    
